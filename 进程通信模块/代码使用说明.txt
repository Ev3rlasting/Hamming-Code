本文件夹内的代码是模拟海明码的进程通信，使用方法须在装有gcc编译环境的Linux系统下运行。
文件夹"Already_compiled"内是已经编译过的文件，开启两个终端窗口进入到该文件夹内，分别在两个终端窗口内输入"./main"和"./recv"便可以运行程序。
操作步骤：
1.首先在recv窗口内键入需要监听的端口，如1234（该端口不能被占用）
2.在main窗口内键入接受方的IP和端口，如127.0.0.1和1234
3.便可在recv窗口内看到接收的结果
